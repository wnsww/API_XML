아이티뱅크(파이썬 기초)
1강 파이썬 설치(설치및 환경설정)
현 환경이 윈도우7 64비트
강의에서는 파이썬2버전으로 강의함
(현재 4월10일기준
세팅 상황
파이썬 3.6.5 64bit
추가 모듈 다운을위한 툴
Anaconda
scrapy설치를 위한 모듈들을 자동적으로 
다운해줌
다만 beatifulsoup4설치는 사용자의 환경에서는 안됨

2강 cmd에서 파이썬 함수 
(
print
내장 함수인 sep
,의 내용 정의
ex)print('바보','니다',sep='입')
	바보입니다

이스케이프 문자
\n=enter
\r=home
\t=space*8
\'=''사이에 문자열 정의
\"=""사이에 문자열 정의
\\=\문자

)

3강 

연산
a = [1,2,3,4]
(이런 형태는 파이썬에서 리스트라함)
최대값 	max(a)=4
최소값 	min(a)=1
합	sum(a)=8
제곱연산pow(2,3)=8(pow(제곱할 수,제곱할 값))
나눗셈	divmod(10,3)=3(몫),1(나머지)

집법표현
bin(100) = Ob(2진법을 나타내는 표시)110010 
oct(100) = Oo(8진법을 나타내는 표시)144 
hex(100) = Ox(16진법을 나타내는 표시)64

기타 연산에 사용되는 함수

반올림 round()=>round(12.562,2)=12.56
앞에는 반올림할수 뒤에는 반올림할 자리수

절대값 abs()=>abs(2)=abs(-2)=2


4강 
기본서식문자
별 영양가 없어서
넘어감
거의 fomat함수로 처리함

5강
작명규칙
이것도 굳이 정리할 필요없음
현재 개발툴 파이참이나
이클립스의 pydev는 문법오류시 바로 알려줌
다만 pydev는 초기에 한글이 들어가면 
오류 발생 그래서 추가적으로 이클립스 환경설정시 
utf-8로 업데이트 시켜줘야함

자료형구조
다음강의에 자세히 설명
넘어감

변수정의
이건 왜 한지 모르것음
기본적으로 변수 정의를 설명
자바완 다르게 변수,변수 = 깂,값으로 해야된다는거 말고는

자료형변환
bool() - 부울형변환
int() - 인트형변환(3버전에는 long과 int 통합됨)
str() -  문자형변환
float() - 실수변환


6강
값입력 받는함수 
input()함수를성명함
딱히 쓸데가없음

7강
산술연산자
+ 덧셈
- 뺄셈
* 곱셈
** 거듭제곱
% 나머지
/ 나누기
// 나누기 몫

비교연산자
자바와 동일
비교연산자
자바와 유사
비교연산자
2진법시 연산 중요하지 많음

8강
randon()함수에 관한 설명
-0.0~1.0사이의 값을 출력하기에
쓸때는 
int(randon()*99+1)-1~100까지
이런식으로 사용해야함

9강 
조건문
파이썬 if문
if 조건식:
elif 조건식:
else:
이런형태로 사용함

10강
함수 def 함수이름():
pass(내용없을시 사용 오류 안나게 하기위해서 사용됨)
return(반환값이 있을시 사용함)
중요한건 가변인자하 해서
def func(a,*ab):
해서 ab변수를 튜플로 변환해서사용된다는게 중요
예를들어
func(a,b,c,d)라하면
함수 안에서는 a와 
ab=(b,c,d)로 받아들임

11강 반복문
자바와 유사하게 돌아감

12강
튜플 
a=(a,b,c)형태로 수정안됨

13강 리스트 
a = [a,b,c] 행태로 수정가능
주로 사용되는 형태로
len(a) 안의 원소수 3개하고 반환된고
range(a)라 하면오류남
주로
range(0,5,1)이런형식으로
0~4까지의 수를 반환하고
세번째는 그 차를 말함따라서
range(0,5,0.1)이라하면
0~4.9까지의 수를 반환한다

14강
리스트 함수
append()-리스트 끝에추가
extend() - 리스트 끝에 각 집합 추가
insert(a,b) - a위치에 b추가
pop([a]) - a위치의 값을 반환후 삭제
remove(a) - a값과 동일한 값을 찾아 삭제
clear() - 모든값 삭제
count(a) - 리스트에서 a와 일치하는 값 반환
index(a) - 리스트에서 인덱스 번호가 a일치하는 값을 찾아 반환
reverse() - 모든값을 뒤집어 나열
sert([reverse=True or False]) - True일때 내림 False일때 오름차순으로 정렬

15강 사전
자바에서 map이랑 유사함
-json형태와 비슷하게
a = {'a':1}형태로 사용
사용시 키값인 
a['a']=1로 나옴

16,17강 앞강의중 함수식과 반복문강의


나름 내용은 알찬데 쓸대없는 부분 설명이랑 강의 중복으로 돈주고 강의
파는데 이렇게 살지 맙시다라는 말이 나올정도로 좋은 강의는 아님
